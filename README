This code will read in a local XML feed from the National Weather Service, write it locally, and then display the data on a web page. User configurable elements include the URL for the nearest local feed, local path to write the data to, and length of interval to refresh data from remote source.